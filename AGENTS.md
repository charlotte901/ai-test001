# Prototype Instructions

Choose layout preference (2026-09-03, tightened after the first pass): the choose screen must match the assessments screen's vertical rhythm, not just avoid the bottom edge. Because the 863px source art alone puts cards at 58% of the page height (vs 45% on assessments), the layout scales against a virtual 1113px design height: `getChooseLayout` uses `min(width/1822, max(height,560)/1113)` and `.choose-layout` starts at `180u` plus centering on `1113u`, with grid margin 33u and 60px bottom padding from the assessment values. Result at every viewport: card top ≈ 35%, card height ≈ 45%, bottom free ≈ 15% — the same proportions as the TEST! page. The three-band strip seams are measured live, so they follow automatically.

Latest choose-step revision (2026-09-03, supersedes the direct login→assessment flow): login submit now opens the CHOOSE! page built from `public/assets/choose-reference.png` (1822 × 863) — three real buttons cropped from the art (测试闯关 / 报告查询 / 个人中心), same SourceCrop + wordmark technique as the assessments screen. Clicking 测试闯关 opens the TEST! assessment page; the assessment back button returns to choose, not home. login → choose keeps the three-band strip pull at the choose-card whitespace; choose ↔ assessments flies whole cards (old cards out, new cards in, staggered); 返回首页 runs the strips in reverse and only releases the flat login surface after they land so the cube can rotate home. Choose cards mirror assessment-card interaction: selection state, check badge, status line, disabled while a transition runs. 报告查询/个人中心 stay selectable-but-closed placeholders.

Latest login form revision (2026-09-03, supersedes “no inputs yet” below): add account and password inputs plus a login submit button inside the front-facing pale-pink screen. Reveal them only after rotation settles. No validation or real authentication: blank fields are allowed, submit clears the form and navigates to the CHOOSE! selection step (see above). Never persist, log, serialize or transmit entered credentials. Keep case playback paused, source shell and 900 ms rotation unchanged. Preserve the existing local preview and hosting metadata; do not publish to a new address as part of this local iteration.

Current login direction (2026-09-03, supersedes the earlier entry flow): remove the content-editing Studio dialog and its visible triggers. Homepage login must rotate the existing cube into a single front-facing 2D surface; do not open assessment selection directly and do not build login inputs yet. Stop every case animation immediately when turning starts and use pale-pink screen faces. Turn and centering share a smooth 900 ms ease-out. Preserve source shell materials but carefully mask corners; no corner wedges or open seams. Reverse restores the homepage cases. The existing TEST! assessment page remains available separately for later integration.

Current carousel direction (2026-09-03): preload the next three-face group while the current group plays. Swap only after an actual rendered scene frame or decoded video frame is ready; keep the old frame during loading. Use at most two layers per screen, pause hidden preloads after two warm-up frames, and pause all media during cube rotation. Preserve the existing 15-second showcase interval unless the user changes it.

Latest transition revision (2026-09-03): do not divide the viewport into equal thirds. Place both horizontal seams in the whitespace above and below the assessment cards, so the complete card row moves intact in the middle band. On phones preserve both rows together. Use these same measured boundaries for outgoing/incoming bands and for the reverse transition.

Latest heading revision (2026-09-03): supersede the YooO! heading below with the bespoke TEST! lettering from `public/assets/test-wordmark-reference.png`. Preserve its exact contours and spacing using source-derived transparency, not a generic font. Leave the four assessment cards and three-band transition intact.

Second-screen direction (2026-09-03): reference `public/assets/assessment-reference.png` governs the black assessment selection screen. Preserve the supplied YooO! graphic and all four card titles/subtitles until the user requests different copy. Clicking the homepage login button is a prototype navigation action, not real authentication. Split the actual current page into three horizontal visual bands (outer bands one direction, center the opposite) with matching incoming bands. Preserve the homepage/custom media state on return; pause hidden media and never mount extra live WebGL scenes for the animation. Keep desktop single-row cards and phone two-column layout. Card selection is real UI state; assessment questions/scoring are not in scope yet.

Product positioning (2026-09-03): this website tests users' ability to use AI, rather than offering generic learning tools. Hero copy should speak about practical tasks, prompting and turning ideas into finished work. Keep a title and short description inside each case screen, but make it quiet and integrated: compact translucent backing, no full-width dark banner, no status dot or prominent border. The assessment flow/scoring is not implemented; do not label the case-viewing CTA as a working test.

User preference (2026-09-03, cases revision): remove the 50k+/120+/98% footer and 10k+ social proof entirely. The cube defaults to automatic rotation through real projects copied from `/Users/lunaecho/Desktop/项目合集_2026-08-31`. Keep originals untouched. Use the Wing It finished Chinese recap, Mario, the existing conbini v2, and both pirate variants. Conbini v1 is mentioned in the collection notes but its folder is absent; do not invent it. Preserve fullscreen responsiveness and independent screen replacement; custom edits suspend the carousel.

User preference (2026-09-03): fill the available browser viewport responsively. Never contain the entire 1536 × 1024 composition inside a centered fixed-aspect-ratio canvas. Anchor navigation/footer to the viewport; scale the original wordmark and cube independently without distortion. Keep existing screen replacement behavior. Very short windows may scroll to preserve readable controls.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.
